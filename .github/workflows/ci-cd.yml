name: Test1 CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-test-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Run tests
      run: |
        echo "Running tests..."
        # Simulate a test (success)
        if [ -f hello.txt ]; then
          echo "Test Passed: hello.txt exists"
        else
          echo "Test Failed"
          exit 1
        fi

    - name: Build project
      run: |
        echo "Building project..."
        # Simulate build by creating build.txt
        echo "Build successful!" > build.txt

    - name: Deploy project
      run: |
        echo "Deploying project..."
        echo "Deployment simulated: CI/CD working!"
